[project]
name = "http-lib"
version = "0.1.0"
description = "Common HTTP library code"
authors = [{ name = "redjax", email = "none@none.com" }]
dependencies = ["httpx>=0.27.2", "hishel>=0.0.33"]
requires-python = "==3.11.*"
readme = "README.md"
license = { text = "MIT" }

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
