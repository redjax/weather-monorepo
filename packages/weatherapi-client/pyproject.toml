[project]
name = "weatherapi-client"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "redjax", email = "no@none.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.scripts]
weatherapi-client = "weatherapi_client:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
